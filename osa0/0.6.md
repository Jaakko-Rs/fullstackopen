# 0.6:
```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note and clicks save
    Note right of browser: JavaScript handles the form submit event
    Note right of browser: Browser prevents default form submission

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    Note left of server: Server saves the new note
    server-->>browser: HTTP 201 Created

    Note right of browser: Browser updates the notes list on the page without reloading